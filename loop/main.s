TEXT ·Loop(SB), $0-32
	MOVQ ·cnt+0*8(FP),AX
	MOVQ ·v0+1*8(FP),BX
	MOVQ ·step+2*8(FP),CX
	MOVQ $0,DX
LOOP_IF:
	CMPQ BX,AX
	JL LOOP_BODY
	MOVQ DX,·r+3*8(FP)
	RET
LOOP_BODY:
	ADDQ BX,BX
	ADDQ CX,DX
	JMP LOOP_IF
	
