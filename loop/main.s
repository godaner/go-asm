#include "textflag.h"
TEXT ·Loop(SB),NOSPLIT,$0-32
	MOVQ ·cnt+0*8(FP),AX
	MOVQ ·v0+1*8(FP),BX
	MOVQ ·step+2*8(FP),CX
	MOVQ $0,DX
LOOP_IF:
	CMPQ DX,AX
	JL LOOP_BODY
	MOVQ BX,·r+3*8(FP)
        RET
LOOP_BODY:
	ADDQ $1,DX
	ADDQ CX,BX
	JMP LOOP_IF
